<h2> This is the show page </h2>
{{#if show.name}}
  <h3>{{show.title}}</h3>
  {{#each show.episodes as |episode index|}}
  <h4>
    <a href="/shows/{{show.name}}/{{episode.title}}">{{episode.title}}</a>
  </h4>
  {{/each}}
{{else}}
  <h3>That show could not be found</h3>
{{/if}}
